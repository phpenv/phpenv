# Configure options for PHP 5.3 releases Darwin specific.
#
if [ -n $PHP_AUTOCONF ]; then
  unset PHP_AUTOCONF
fi

CONFIGURE_OPTIONS="--enable-cli
                   --enable-pcntl
                   --enable-phar"

MANUAL_EXTENSIONS=()
